{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mesh-lab.serviceAccountName" . }}
  labels:
    {{- include "mesh-lab.labels" . | nindent 4 }}
{{- end }}
