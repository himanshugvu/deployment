apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mesh-lab.fullname" . }}-test-connection"
  labels:
    {{- include "mesh-lab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['-qO-', 'http://{{ include "mesh-lab.fullname" . }}:{{ .Values.service.port }}/api/status']
  restartPolicy: Never
