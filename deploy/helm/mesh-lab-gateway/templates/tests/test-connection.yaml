apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mesh-lab-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "mesh-lab-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['-qO-', 'http://{{ include "mesh-lab-gateway.fullname" . }}:{{ .Values.service.port }}/actuator/health/readiness']
  restartPolicy: Never
