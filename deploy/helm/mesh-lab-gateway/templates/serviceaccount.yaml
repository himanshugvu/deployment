{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mesh-lab-gateway.serviceAccountName" . }}
  labels:
    {{- include "mesh-lab-gateway.labels" . | nindent 4 }}
{{- end }}
